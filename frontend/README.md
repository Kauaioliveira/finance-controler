# Frontend

Painel React para importar CSVs financeiros, revisar categorização e acompanhar relatórios.

Rodar localmente:

```powershell
cd frontend
npm.cmd install
npm.cmd run dev
```

Variável principal:

```env
VITE_API_BASE_URL=http://127.0.0.1:8010
```
